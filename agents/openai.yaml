interface:
  display_name: "KAIC Research Skills"
  short_description: "Research stats, reporting, and KCHS routing"
  default_prompt: "Use $kaic-research-skills to plan or interpret research analyses, generate APA tables, verify references, and route KCHS work to $kaic-chs-analysis."

policy:
  allow_implicit_invocation: true
